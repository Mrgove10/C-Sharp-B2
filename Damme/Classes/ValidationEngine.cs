namespace Damme
{
    public class ValidationEngine : IValidation
    {
        public bool Validate(Pion pionToMove, int xTo, int yTo, Pion[,] mainField, char player)
        {
            /*if(pionToMove.PlayerSign = )
            if*/
            return false;
        }
    }
}